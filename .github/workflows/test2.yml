name: Tufuwu/test2
on:
  push:
    branches:
    - "**/*"
  pull_request:
concurrency:
#   # This item has no matching transformer
#   maximum_number_of_builds: 0
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - run: python -m pip install --upgrade pip
    - run: python -m pip install -v .
    - run: python -m pip install $PRE --upgrade pytest pytest-cov coverage
    - run: coverage run run_tests.py
    - run: coverage report -m
    - run: bash <(curl -s https://codecov.io/bash)
      if: "${{ success() }}"
    strategy:
      matrix: {}
#       # 'allow_failures' transformations are currently unsupported.
  test_2:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - uses: actions/setup-python@v5.0.0
      with:
        python-version: 2.7
    - run: python -m pip install --upgrade pip
    - run: python -m pip install -v .
    - run: python -m pip install $PRE --upgrade pytest pytest-cov coverage
    - run: coverage run run_tests.py
    - run: coverage report -m
    - run: bash <(curl -s https://codecov.io/bash)
      if: "${{ success() }}"
  test_3:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - uses: actions/setup-python@v5.0.0
      with:
        python-version: 3.4
    - run: python -m pip install --upgrade pip
    - run: python -m pip install -v .
    - run: python -m pip install $PRE --upgrade pytest pytest-cov coverage
    - run: coverage run run_tests.py
    - run: coverage report -m
    - run: bash <(curl -s https://codecov.io/bash)
      if: "${{ success() }}"
  test_4:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - uses: actions/setup-python@v5.0.0
      with:
        python-version: 3.5
    - run: python -m pip install --upgrade pip
    - run: python -m pip install -v .
    - run: python -m pip install $PRE --upgrade pytest pytest-cov coverage
    - run: coverage run run_tests.py
    - run: coverage report -m
    - run: bash <(curl -s https://codecov.io/bash)
      if: "${{ success() }}"
  test_5:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - uses: actions/setup-python@v5.0.0
      with:
        python-version: 3.6
    - run: python -m pip install --upgrade pip
    - run: python -m pip install -v .
    - run: python -m pip install $PRE --upgrade pytest pytest-cov coverage
    - run: coverage run run_tests.py
    - run: coverage report -m
    - run: bash <(curl -s https://codecov.io/bash)
      if: "${{ success() }}"
  test_6:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - uses: actions/setup-python@v5.0.0
      with:
        python-version: 3.7
    - run: python -m pip install --upgrade pip
    - run: python -m pip install -v .
    - run: python -m pip install $PRE --upgrade pytest pytest-cov coverage
    - run: coverage run run_tests.py
    - run: coverage report -m
    - run: bash <(curl -s https://codecov.io/bash)
      if: "${{ success() }}"
  test_7:
    runs-on: ubuntu-latest
    env:
      PRE: "--pre"
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - uses: actions/setup-python@v5.0.0
      with:
        python-version: nightly
    - run: python -m pip install --upgrade pip
    - run: python -m pip install -v .
    - run: python -m pip install $PRE --upgrade pytest pytest-cov coverage
    - run: coverage run run_tests.py
    - run: coverage report -m
    - run: bash <(curl -s https://codecov.io/bash)
      if: "${{ success() }}"
